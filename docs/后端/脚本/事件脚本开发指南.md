# GMS 事件脚本开发指南

## 1. 概述

本文档旨在为开发者提供一个关于如何编写、维护和优化 GMS (Game Maple Story) 事件脚本的全面指南。事件脚本是实现各种游戏内活动、副本、Boss 战和自定义内容的核心。遵循本指南将有助于您编写出高效、稳定且易于维护的脚本。

## 2. 脚本环境与语法标准

我们的脚本引擎基于 **GraalVM JS**，完全支持 **ECMAScript 2024 (ES15)** 标准。这意味着您可以使用所有现代 JavaScript 的特性来编写更简洁、更强大的代码。

我们强烈推荐您使用现代语法，例如：

- **`const` 和 `let`**: 代替 `var` 来声明变量，以获得块级作用域和更清晰的意图。
- **箭头函数 (`=>`)**: 用于编写更简洁的函数表达式。
- **模板字符串 (`` ` ``)**: 使用反引号轻松拼接字符串和变量，例如 `` `你好, ${player.getName()}` ``。
- **解构赋值**: 快速从对象或数组中提取值。
- **类 (`class`)**: 使用面向对象的方式组织复杂逻辑。

**语法示例：**
```javascript
// 使用 const/let 定义常量和变量
const MAX_PLAYERS = 6;
let currentStage = 1;

// 使用模板字符串和箭头函数
const welcomeMessage = (player) => `欢迎, ${player.getName()}! 当前是第 ${currentStage} 阶段。`;

// 解构赋值
const [leader, ...members] = eim.getPlayers().toArray(); // 获取队长和其余成员
```

## 3. 核心概念与运行流程

### 3.1 核心对象

- **`em` (EventManager)**: 事件管理器。每个事件脚本（如 `Zakum.js`）都拥有一个独立的 `em` 实例。
- **`eim` (EventInstanceManager)**: 事件实例管理器。代表一个具体的、正在运行的事件副本。
- **`player` (Character)**: 玩家角色对象。

### 3.2 运行流程

1.  **服务器启动加载**: `EventScriptManager` 加载所有位于 `scripts/event` 目录下的 `.js` 文件。
2.  **注入与初始化**: 为每个脚本创建 `EventManager` 实例，并以 `em` 的名称注入，然后调用脚本的 `init()` 函数。
3.  **事件触发**: 玩家与 NPC 对话，NPC 脚本调用 `em.startInstance(...)`。
4.  **实例创建 (`setup`)**: `em` 调用事件脚本中的 `setup()` 函数，此函数**必须**返回一个 `EventInstanceManager` (`eim`) 对象。
5.  **事件开始**: Java 端获取 `eim`，注册参与者，并调用 `eim.startEvent()`，触发 `playerEntry` 等函数。
6.  **脚本逻辑执行**: 脚本通过 `eim` 控制事件流程。
7.  **事件结束 (`dispose`)**: 脚本**必须**调用 `eim.dispose()` 来结束事件并清理所有资源。

## 4. 标准事件脚本范本

<details open>
<summary>点击展开/折叠代码</summary>

````javascript
// 全局变量区域
// 建议将常用的地图ID、怪物ID、物品ID等定义为常量，便于维护。
const ENTRY_MAP = 910000000; // 入口地图
const EVENT_MAP = 105040400; // 事件地图
const BOSS_ID = 8800000;     // Boss 怪物 ID

/**
 * 初始化函数 (init)
 * 服务器启动时或重载脚本时调用。
 * 通常用于设置全局定时任务。
 * @param {null} eim - 在init阶段，eim通常为null。
 */
function init(eim) {
    // em.scheduleAtTimestamp("globalTask", getNextMidnight()); 
}

/**
 * 实例设置函数 (setup)
 * 当一个新事件副本被请求时调用。
 * 【必须】返回一个 EventInstanceManager (eim) 对象。
 * @param {number} difficulty - 难度系数（可选）
 * @param {number} lobbyId - 大厅ID（由系统分配）
 * @returns {EventInstanceManager} - 新创建的事件实例
 */
function setup(difficulty, lobbyId) {
    const eim = em.newInstance("MyEvent_" + lobbyId);
    eim.setProperty("stage", "0");
    return eim;
}

/**
 * 玩家进入事件地图时调用 (playerEntry)
 * @param {EventInstanceManager} eim - 当前事件的实例
 * @param {Character} player - 进入的玩家角色对象
 */
function playerEntry(eim, player) {
    const map = eim.getMapInstance(EVENT_MAP);
    player.changeMap(map, map.getPortal(0));
    eim.setClock(30 * 60); // 30分钟倒计时
    eim.schedule("spawnPhase1", 5000);
}

/**
 * 玩家复活时调用 (playerRevive)
 * @param {EventInstanceManager} eim - 当前事件的实例
 * @param {Character} player - 复活的玩家
 */
function playerRevive(eim, player) {
    player.addHP(50);
    const map = em.getChannelServer().getMapFactory().getMap(ENTRY_MAP);
    player.changeMap(map, map.getPortal(0));
    eim.unregisterPlayer(player);
}

/**
 * 玩家断开连接时调用 (playerDisconnected)
 * @param {EventInstanceManager} eim - 当前事件的实例
 * @param {Character} player - 断开连接的玩家
 */
function playerDisconnected(eim, player) {
    // 【重要】<span style="color:red; font-weight:bold;">内存安全</span>: 必须从事件中移除断线的玩家，以防内存泄漏。
    eim.unregisterPlayer(player);
}

/**
 * 队伍中有玩家被踢出时调用 (partyMemberRemoved)
 * @param {EventInstanceManager} eim - 当前事件的实例
 * @param {Character} player - 被踢出的玩家
 */
function partyMemberRemoved(eim, player) {
    // 【重要】<span style="color:red; font-weight:bold;">内存安全</span>: 同上，也需要将玩家从事件中移除。
    eim.unregisterPlayer(player);
}

/**
 * 怪物被击杀时调用 (monsterValue)
 * @param {EventInstanceManager} eim - 当前事件的实例
 * @param {number} mobId - 被击杀的怪物ID
 */
function monsterValue(eim, mobId) {
    if (mobId === BOSS_ID) {
        eim.broadcastPlayerMsg(5, "恭喜你们，挑战成功！");
        eim.getPlayers().forEach(p => eim.giveReward(p, 4001000, 1));
        eim.schedule("finishEvent", 3000);
    }
}

/**
 * 玩家在地图上移动时调用 (playerMovement)
 * <span style="color:orange; font-weight:bold;">CPU 性能</span>: 此函数调用非常频繁，请谨慎使用，避免复杂计算。
 */
function playerMovement(eim, player, move) {
    // if (player.getPosition().y > 500) { player.setHp(0); }
}

// =================================================
//          自定义函数区域
// =================================================

function spawnPhase1(eim) {
    const map = eim.getMapInstance(EVENT_MAP);
    for (let i = 0; i < 10; i++) {
        const mob = em.getMonster(9300000);
        eim.spawnMonsterOnGroundBelow(mob, new java.awt.Point(100, 100));
    }
    eim.setProperty("stage", "1");
}

function finishEvent(eim) {
    eim.warpAllPlayer(ENTRY_MAP);
    // 【必须】<span style="color:red; font-weight:bold;">内存安全</span>: 调用 dispose 来清理资源。
    eim.dispose();
}

/**
 * 当脚本被重载或服务器关闭时调用 (cancelSchedule)
 */
function cancelSchedule(eim) {
    // <span style="color:red; font-weight:bold;">内存安全</span>: 如果在 init 中设置了长期运行的全局定时任务，在此处取消。
    // if (eim != null) { eim.cancel(); }
}
````
</details>

## 5. 核心 API 列表
(内容同前，此处省略以保持简洁)

## 6. NPC 脚本调用事件范本 (NextLevel 语法)

在很多情况下，事件是由玩家与 NPC 对话触发的。以下是在 NPC 脚本中如何使用 `NextLevel` 语法正确启动一个事件的范本。

<details open>
<summary>点击展开/折叠代码</summary>

````javascript
// NPC 脚本入口
function start() {
    levelMain(); 
}

// 用于结束或异常处理
function level() {
    cm.dispose();
}

// 主对话逻辑
function levelMain() {
    cm.sendYesNoLevel("End", "ConfirmStart", "你想开始【XXX】组队任务吗？");
}

// 确认开始事件
function levelConfirmStart() {
    // 检查玩家是否是队长
    if (!cm.isLeader()) {
        cm.sendOk("请让你的队长来和我说话。");
        cm.dispose();
        return;
    }

    // 检查队伍人数等其他条件...
    if (cm.getParty().getMembers().size() < 2) {
        cm.sendOk("你需要至少2名队员才能开始任务。");
        cm.dispose();
        return;
    }

    // 获取事件管理器
    const eventName = "MyEvent"; // 对应事件脚本的文件名 MyEvent.js
    const em = cm.getEventManager(eventName);

    if (em == null) {
        cm.sendOk("当前任务正在维护中，请稍后再试。");
        cm.dispose();
        return;
    }

    // 启动事件实例
    // em.startInstance(party, map) 是为组队任务设计的标准启动方式
    // 它会自动在 MyEvent.js 中调用 setup() 函数，并注册队伍
    const started = em.startInstance(cm.getPlayer().getParty(), cm.getPlayer().getMap());
    
    if (!started) {
        cm.sendOk("当前频道任务繁忙，请稍后再试或更换频道。");
    }
    
    // NPC 对话结束
    cm.dispose();
}

// 玩家选择 "否" 后的逻辑
function levelEnd() {
    cm.sendOk("如果你改变主意了，可以再来找我。");
    cm.dispose();
}
````
</details>

## 7. 性能与安全规范

### 7.1 <span style="color:red; font-weight:bold;">内存安全</span>

1.  **必须调用 `eim.dispose()`**: 任何事件，无论成功、失败或中途取消，都必须有逻辑路径能最终调用 `eim.dispose()`。这是释放地图、玩家、怪物等所有相关资源的关键。如果忘记调用，会导致这些对象常驻内存，引发严重的内存泄漏。
2.  **处理玩家离线**: 必须实现 `playerDisconnected` 和 `partyMemberRemoved` 函数，并在其中调用 `eim.unregisterPlayer(player)`。否则，断线的玩家对象将永远被 `eim` 引用，导致其占用的内存无法被回收。
3.  **正确取消定时任务**: 如果使用 `em.schedule` 创建了长期运行的全局任务，请务必在 `cancelSchedule` 函数中提供取消逻辑，以防脚本重载时产生重复的任务。对于 `eim.schedule` 创建的实例级任务，它们会在 `eim.dispose()` 时自动取消，无需手动处理。

### 7.2 <span style="color:orange; font-weight:bold;">CPU 性能</span>

1.  **谨慎使用高频函数**: `playerMovement` 函数会在玩家每次移动时被调用，频率极高。应避免在此函数中执行任何复杂或耗时的操作（如数据库查询、大量循环等）。
2.  **避免死循环和长循环**: 确保所有 `for` 和 `while` 循环都有明确的退出条件。在循环中避免进行网络或文件 I/O 等阻塞操作。
3.  **合理使用 `schedule`**: 对于需要等待的操作，应使用 `eim.schedule` 或 `em.schedule` 将其分解为多个异步步骤，而不是使用 `Thread.sleep()` 或忙等待（空循环）。`Thread.sleep()` 会阻塞整个脚本引擎线程，影响所有在该线程上运行的脚本。

    ```javascript
    // 错误示范：阻塞线程
    function badExample(eim) {
        eim.spawnMonster(...);
        java.lang.Thread.sleep(5000); // 阻塞了脚本线程5秒！严重影响性能！
        eim.spawnMonster(...);
    }

    // 正确示范：使用 schedule 异步执行
    function goodExample(eim) {
        eim.spawnMonster(...);
        eim.schedule("spawnNextWave", 5000); // 5秒后执行下一个函数，不阻塞当前线程
    }

    function spawnNextWave(eim) {
        eim.spawnMonster(...);
    }
    ```

### 7.3 <span style="color:blue; font-weight:bold;">线程安全</span>

- **共享引擎机制**: 系统底层采用的 GraalVM 共享引擎 (`SHARED_ENGINE`) 机制保证了不同脚本的 `Context` 是隔离的，但它们共享 JIT 等资源。
- **`SynchronizedInvocable`**: 系统已将脚本调用接口包装为线程安全的 `SynchronizedInvocable`，这意味着从 Java 端并发调用同一个脚本的不同函数是安全的。
- **脚本内部状态**: 尽管如此，您仍需注意脚本内部的全局变量。如果多个函数可能并发地修改同一个 JS 全局变量，您需要自行处理同步问题。**最佳实践**是使用 `eim.setProperty` 来管理状态，而不是依赖 JS 全局变量，因为 `eim` 内部对属性的读写是线程安全的。

通过遵循以上指南，您将能够创建出更加健壮和高效的事件脚本，为玩家带来流畅和丰富的游戏体验。
