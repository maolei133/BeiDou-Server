<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" shutdownHook="disable">
    <Properties>
        <Property name="LOG_EXCEPTION_CONVERSION_WORD">%xwEx</Property>
        <Property name="LOG_LEVEL_PATTERN">%5p</Property>
        <Property name="LOG_DATEFORMAT_PATTERN">yyyy-MM-dd HH:mm:ss.SSS</Property>
        <Property name="CONSOLE_LOG_PATTERN">%clr{%d{${LOG_DATEFORMAT_PATTERN}}}{faint} %clr{${LOG_LEVEL_PATTERN}} %clr{%pid}{magenta} %clr{---}{faint} %clr{[%15.15t]}{faint} %clr{%-40.40c{1.}}{cyan} %clr{:}{faint} [traceId=%X{traceId} module=%X{module} action=%X{action} result=%X{result} accountId=%X{accountId} characterId=%X{characterId} world=%X{world} channel=%X{channel} mapId=%X{mapId} opcode=%X{opcode} scriptPath=%X{scriptPath} wzPath=%X{wzPath} costMs=%X{costMs} exceptionClass=%X{exceptionClass}] %m%n${sys:LOG_EXCEPTION_CONVERSION_WORD}</Property>
        <Property name="FILE_LOG_PATTERN">%d{${LOG_DATEFORMAT_PATTERN}} ${LOG_LEVEL_PATTERN} %pid --- [%t] %-40.40c{1.} : [traceId=%X{traceId} module=%X{module} action=%X{action} result=%X{result} accountId=%X{accountId} characterId=%X{characterId} world=%X{world} channel=%X{channel} mapId=%X{mapId} opcode=%X{opcode} scriptPath=%X{scriptPath} wzPath=%X{wzPath} costMs=%X{costMs} exceptionClass=%X{exceptionClass}] %m%n${sys:LOG_EXCEPTION_CONVERSION_WORD}</Property>
        
        <!-- 禁用JMX -->
        <Property name="log4j2.disable.jmx">true</Property>
        <Property name="log4j2.disableShutdownHook">true</Property>
    </Properties>
    
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="${CONSOLE_LOG_PATTERN}"/>
        </Console>
        <!-- Monitor events: JSON lines intended for Loki/Promtail; file-only to keep periodic samples out of the console. -->
        <RollingFile name="MonitorEventLog" fileName="logs/audit/monitor.json"
                     filePattern="logs/audit/monitor-%d{yyyy-MM-dd}.json.gz">
            <PatternLayout pattern="%m%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy />
            </Policies>
        </RollingFile>
    </Appenders>
    
    <Loggers>
        <!-- 关闭 Druid 日志输出 -->
        <Logger name="com.alibaba.druid" level="ERROR"/>
        
        <!-- 关闭 flyway 日志输出 -->
        <Logger name="org.flywaydb" level="ERROR"/>

        <!-- Monitor events: JSON lines intended for Loki/Promtail; file-only to keep periodic samples out of the console. -->
        <Logger name="monitor-event" level="INFO" additivity="false">
            <AppenderRef ref="MonitorEventLog"/>
        </Logger>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>